{
  "name": "personal-page",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "build": "node scripts/build.mjs",
    "site": "npm run sync && npm run build",
    "preview": "BASE_PATH=/ npm run build && npx --yes serve public",
    "test": "node --test scripts/lib/html.test.mjs scripts/lib/page-context.test.mjs scripts/lib/site-config.test.mjs scripts/lib/sitemap.test.mjs scripts/build.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "googleapis": "^172.0.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
