{
  "name": "personal-page",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "build": "node scripts/build.mjs",
    "site": "npm run sync && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "googleapis": "^144.0.0"
  }
}
